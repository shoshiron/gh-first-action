name: First Workflow
on: workflow_dispatch
jobs:
  first-job: # Not reserved name
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello world !!!!"
      - name: Print goodbye
        run: echo "Done - goodbye !!!"
